import { createUser, createUserNonce } from "./createUser";

const main = async () => {
  try {
    // await createUserNonce();
    // await createUser();
  } catch (error) {
    console.log(error);
  }
};

main();
